## Comments

A line whose first non-whitespace character is `#` is ignored by the
compiler:

```
# This line is ignored by the compiler.
```

> Watch out: a `#` in the *middle* of a line is not a comment — it stays in
> the text. Write comments on their own line.
